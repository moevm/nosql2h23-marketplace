class RouterLinks {
    main = "/main"
    auth = "/"
    registration = "/reg"
    game_page = "/game_page"
    user_page = "/user_page"
}

let routerLinks = new RouterLinks()

export default routerLinks;